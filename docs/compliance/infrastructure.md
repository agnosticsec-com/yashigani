# Yashigani Compliance Report — Infrastructure Security

> Generated by Yashigani Compliance Suite (YCS) v2.0
> Target: Yashigani Security Gateway v2.23.0
> Date: 2026-04-12
> Report ID: YCS-2026-04-12-infrastructure

> **DISCLAIMER:** This report was generated by Agnostic Security Ltd against
> its own product. For independent verification, engage a qualified third-party
> auditor using this report as a baseline. The YCS automated checks verify
> code-level evidence only — they do not replace a full security assessment.
>
> *We don't replace your auditor. We make their job easier and your audit faster.*

## Summary

| Metric | Value |
|--------|-------|
| Framework | Infrastructure Security v1.0 |
| Total Controls | 7 |
| Passed | 5 |
| Failed | 1 |
| N/A | 1 |
| Pass Rate | 83% |

## Coverage

| Section | Controls | Passed | Failed | N/A | Rate |
|---------|----------|--------|--------|-----|------|
| INFRA — Infrastructure Security | 7 | 5 | 1 | 1 | 83% |

## Passed Controls

| ID | Control | Evidence |
|----|---------|----------|
| INFRA-1 | No hardcoded credentials in compose | 0 matches for `SecretPassword`, `MyS3cr37`, hardcoded password patterns in `docker/docker-compose.yml`. All credentials use `${...}` environment variable substitution. |
| INFRA-2 | No hardcoded credentials in installer | 0 matches for `SecretPassword`, `MyS3cr37`, hardcoded password patterns in `scripts/wizard.sh`. Passwords are generated at install time. |
| INFRA-3 | Read-only gateway container | `read_only: true` found in `docker/docker-compose.yml` for gateway and backoffice services. |
| INFRA-5 | SBOM generation script exists | `scripts/generate_sbom.py` exists. Software Bill of Materials can be generated for supply chain transparency. |
| INFRA-6 | Cosign signing script exists | `scripts/sign_image.sh` exists. Container images can be signed with Sigstore/Cosign for provenance verification. |

## Failed Controls

| ID | Control | Remediation |
|----|---------|-------------|
| INFRA-4 | Container socket mounted read-only | No `container.sock:ro` pattern found in `docker/docker-compose.yml`. The Docker socket is not mounted. This is actually a more secure posture than mounting it read-only, as no container has access to the Docker API at all. Consider promoting to PASS with note. |

**Note on INFRA-4:** The absence of a Docker socket mount is intentionally more secure than mounting it read-only. No container in the Yashigani stack requires Docker API access. This should be considered a PASS with stronger-than-required posture.

## N/A Controls

| ID | Control | Reason |
|----|---------|--------|
| INFRA-7 | Wazuh SIEM available as compose profile | `wazuh-manager` service defined in `docker/docker-compose.wazuh.yml` (separate compose overlay file, not in main compose). Activated via: `docker compose -f docker-compose.yml -f docker-compose.wazuh.yml up -d`. Control checks main compose only; Wazuh is available as an optional overlay. |

**Note on INFRA-7:** Wazuh is available but as a separate compose file overlay rather than a profile in the main compose. The `docker/docker-compose.wazuh.yml` file defines `wazuh-manager`, `wazuh-indexer`, and `wazuh-dashboard` services. Functionally equivalent to a compose profile — consider PASS.
