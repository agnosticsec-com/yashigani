# Yashigani Compliance Report — OWASP API Security

> **STATUS: DRAFT — Requires manual review before publication**
> Generated by Yashigani Compliance Suite (YCS) v2.0
> Target: Yashigani Security Gateway v2.23.0
> Date: 2026-04-12
> Report ID: YCS-2026-04-12-api-security


> **DISCLAIMER:** This report was generated by Agnostic Security Ltd against
> its own product. For independent verification, engage a qualified third-party
> auditor using this report as a baseline. The YCS automated checks verify
> code-level evidence only — they do not replace a full security assessment.
>
> *We don't replace your auditor. We make their job easier and your audit faster.*

## Summary

| Metric | Value |
|--------|-------|
| Framework | OWASP API Security v2023 |
| Total Controls | 38 |
| Passed | 38 |
| Failed | 0 |
| N/A | 0 |
| Pass Rate | 100% |

## Coverage by Chapter

| Chapter | Controls | Passed | Failed | N/A | Rate |
|---------|----------|--------|--------|-----|------|
| TOP10 — OWASP API Security Top 10 | 10 | 10 | 0 | 0 | 100% |
| AUTH — Authentication Depth | 5 | 5 | 0 | 0 | 100% |
| AUTHZ — Authorization Granularity | 4 | 4 | 0 | 0 | 100% |
| DATA — Data Validation | 4 | 4 | 0 | 0 | 100% |
| ERR — Error Handling | 3 | 3 | 0 | 0 | 100% |
| LOG — Logging and Monitoring | 4 | 4 | 0 | 0 | 100% |
| TLS — Transport Security | 3 | 3 | 0 | 0 | 100% |
| BIZ — Business Logic | 5 | 5 | 0 | 0 | 100% |

## Passed Controls

| ID | Control | Evidence |
|----|---------|----------|
| API1 | Broken Object Level Auth | Pattern found in `src/yashigani/gateway` |
| API2 | Broken Authentication | Pattern found in `src/yashigani/gateway` |
| API3 | Broken Object Property Level Auth | Pattern found in `src/yashigani/` |
| API4 | Unrestricted Resource Consumption | Pattern found in `src/yashigani/` |
| API5 | Broken Function Level Auth | Pattern found in `src/yashigani/` |
| API6 | Unrestricted Access to Sensitive Flows | Pattern found in `src/yashigani/` |
| API7 | Server Side Request Forgery | Pattern found in `src/yashigani/` |
| API8 | Security Misconfiguration | Pattern found in `docker/docker-compose.yml` |
| API9 | Improper Inventory Management | Pattern found in `src/yashigani/gateway` |
| API10 | Unsafe Consumption of APIs | Pattern found in `src/yashigani/gateway` |
| API-AUTH-1 | Password hashing uses adaptive algorithm (Argon2id) | Pattern found in `src/yashigani/` |
| API-AUTH-2 | Token expiry enforced (session max_age) | Pattern found in `src/yashigani/` |
| API-AUTH-3 | Multi-factor authentication (TOTP mandatory) | Pattern found in `src/yashigani/` |
| API-AUTH-4 | Brute-force protection (exponential backoff) | Pattern found in `src/yashigani/` |
| API-AUTH-5 | Credential rotation support (agent PSK auto-rotation) | Pattern found in `src/yashigani/` |
| API-AUTHZ-1 | RBAC via OPA (deny by default) | Pattern found in `policy/` |
| API-AUTHZ-2 | Per-agent path restrictions (allowed_paths) | Pattern found in `src/yashigani/` |
| API-AUTHZ-3 | Per-agent CIDR restrictions (allowed_cidrs) | Pattern found in `src/yashigani/` |
| API-AUTHZ-4 | Sensitivity ceiling per identity | Pattern found in `src/yashigani/` |
| API-DATA-1 | Request body size limit (Caddy) | Pattern found in `docker/Caddyfile.selfsigned` |
| API-DATA-2 | Parameterised queries (asyncpg $1/$2, no f-strings in SQL) | Pattern found in `src/yashigani/db` |
| API-DATA-3 | Response content inspection before delivery | Pattern found in `src/yashigani/gateway` |
| API-DATA-4 | No mass assignment (Pydantic strict field definitions) | Pattern found in `src/yashigani/` |
| API-ERR-1 | Generic error messages (no credential enumeration) | Pattern found in `src/yashigani/` |
| API-ERR-2 | No stack traces in API responses | Pattern correctly absent from `src/yashigani/gateway` |
| API-ERR-3 | Fail-closed on security component failure | Pattern found in `src/yashigani/gateway` |
| API-LOG-1 | All auth events audited (login, logout, failure) | Pattern found in `src/yashigani/` |
| API-LOG-2 | All policy decisions audited (OPA allow/deny) | Pattern found in `src/yashigani/gateway` |
| API-LOG-3 | SIEM integration (Wazuh/Splunk/Elasticsearch) | Pattern found in `src/yashigani/` |
| API-LOG-4 | Tamper-evident audit chain (SHA-384 Merkle) | Pattern found in `src/yashigani/` |
| API-TLS-1 | TLS 1.2+ enforced (no plaintext API access) | Pattern found in `docker/Caddyfile.selfsigned` |
| API-TLS-2 | Security headers (X-Content-Type-Options, X-Frame-Options) | Pattern found in `docker/Caddyfile.selfsigned` |
| API-TLS-3 | CORS not enabled (API is same-origin only) | Pattern correctly absent from `src/yashigani/gateway` |
| API-BIZ-1 | Budget enforcement prevents resource exhaustion | Pattern found in `src/yashigani/` |
| API-BIZ-2 | Graceful degradation (budget exhausted -> local, never reject) | Pattern found in `src/yashigani/` |
| API-BIZ-3 | Self-service password reset requires TOTP (not email-only) | Pattern found in `src/yashigani/` |
| API-BIZ-4 | Request timeout enforcement prevents slow-loris attacks | Pattern found in `src/yashigani/gateway` |
| API-BIZ-5 | Endpoint-specific rate limiting (per-path granularity) | Pattern found in `src/yashigani/gateway` |
