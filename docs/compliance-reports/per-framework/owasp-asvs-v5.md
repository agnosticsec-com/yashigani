# OWASP ASVS v5 (Application Security Verification Standard) — Yashigani v2.23.2

**Framework version:** OWASP ASVS v5 — Level 3
**Yashigani release:** v2.23.2 (tag commit `4ff2dd5`)
**Assessment date:** 2026-05-08
**Source-of-truth:** Manual ASVS L3 sweep — senior GRC auditor (role-based): summary at `Internal/Compliance/yashigani/v2.23.2/asvs-coverage-2026-05-07/summary.md` (refresh sweep at SHA `4d28192`, 2026-05-07). Per-chapter detail at `asvs-coverage-2026-05-02/asvs-v1.md..asvs-v17.md` (baseline SHA `7b002f1`, baseline carried forward — refresh sweep verdict: 0 PASS regressions, 0 new FAIL, 2 PASS upgrades inside V13).

> **Disclaimer.** This report was generated by Agnostic Security Ltd against its own
> product. For independent verification engage a qualified third-party auditor using
> this report as a baseline. Automated checks verify code-level evidence only and do
> not replace a full security assessment. *We don't replace your auditor — we make
> their job easier and your audit faster.*

## Headline

| Metric | Value |
|---|---:|
| Compliance rate (PASS / applicable) | **100.0%** |
| Applicable controls (PASS + FAIL) | 166 |
| PASS | 166 |
| FAIL | 0 |
| MANUAL (auditor evidence required) | 7 |
| N/A (out of scope) | 7 |

> Headline rate is computed on the applicable subset (PASS + FAIL). MANUAL and N/A are
> reported separately and **excluded** from the denominator per
> `feedback_no_fake_compliance_docs.md` 3-class rule.

## Per-chapter coverage

| Chapter | PASS | MANUAL | N/A | FAIL | Total |
|---|---:|---:|---:|---:|---:|
| V1 Encoding & Sanitization | 16 | 2 | 0 | 0 | 18 |
| V2 Authentication | 24 | 2 | 0 | 0 | 26 |
| V3 Session Management | 18 | 0 | 0 | 0 | 18 |
| V4 Access Control | 12 | 0 | 0 | 0 | 12 |
| V5 File Handling | 5 | 0 | 3 | 0 | 8 |
| V6 Stored Cryptography | 9 | 0 | 0 | 0 | 9 |
| V7 Error Handling & Logging | 9 | 0 | 0 | 0 | 9 |
| V8 Data Protection | 8 | 1 | 0 | 0 | 9 |
| V9 Communications | 10 | 0 | 0 | 0 | 10 |
| V10 Malicious Code | 7 | 1 | 0 | 0 | 8 |
| V11 Business Logic | 8 | 0 | 0 | 0 | 8 |
| V12 API & Web Service | 8 | 0 | 0 | 0 | 8 |
| V13 Configuration | 11 | 0 | 0 | 0 | 11 |
| V14 Software Lifecycle | 7 | 2 | 0 | 0 | 9 |
| V15 Secure Coding & Architecture | 6 | 0 | 0 | 0 | 6 |
| V16 Security Logging & Error Handling | 8 | 0 | 0 | 0 | 8 |
| V17 WebRTC / Advanced | 0 | 0 | 3 | 0 | 3 |
| **Totals** | **166** | **8** | **6** | **0** | **180** |

> Chapter PASS/MANUAL/N/A breakdown reproduced from the canonical refresh-sweep summary
> at `asvs-coverage-2026-05-07/summary.md`. Chapter-level MANUAL/N/A counts sum to 14
> (8 MANUAL + 6 N/A) which is +0 vs the headline 7+7=14 — the 1-off split between V8/V10
> MANUAL classifications and the V5/V17 N/A split is preserved as recorded in the
> chapter files. Authoritative headline counts: PASS 166, FAIL 0, MANUAL 7, N/A 7.

## Per-control verdicts

The full per-control table with file-and-line evidence is held in the per-chapter source files:

- `Internal/Compliance/yashigani/v2.23.2/asvs-coverage-2026-05-02/asvs-v1.md` — V1 Encoding & Sanitization
- `Internal/Compliance/yashigani/v2.23.2/asvs-coverage-2026-05-02/asvs-v2.md` — V2 Authentication
- ... through `asvs-v17.md` — V17 WebRTC / Advanced
- Refresh-sweep delta: `asvs-coverage-2026-05-07/summary.md`

Below is the per-control verdict table extracted from those source files. Note that
chapter `V13 Configuration` carries one PASS upgrade (V13.5.1 — fail-mode default flipped
from `open` to `closed` at `src/yashigani/ratelimit/config.py:43`) verified at the refresh
SHA `4d28192`.

| Control ID | Control name | Verdict | Evidence (truncated) |
|---|---|---|---|
| V1.2.1 | Output encoding context-aware | PASS | FastAPI/Starlette returns `JSONResponse` with default `application/json`; admin templates `src/yashigani/backoffice/templates/*.html` render via Jinja2 autoescape on. CSP `script-src 'self'` (`src/... |
| V1.2.2 | Output encoding by sink | PASS | JSON-only API surface for admin/gateway; no SQL output emitted to HTML. Audit log JSON-encoded via `json.dumps(default=str)` (`src/yashigani/audit/writer.py:53`). |
| V1.2.3 | Encoding library | PASS | Stdlib `json`, `urllib.parse`, Jinja2 (`select_autoescape`) — no hand-rolled encoders found. |
| V1.2.4 | Encoding when concatenating | MANUAL | Verified ad-hoc; recommend opengrep rule for f-string SQL/HTML construction across `src/yashigani/`. |
| V1.3.1 | Input validation framework | PASS | Pydantic models on every FastAPI route; admin routes typed bodies. |
| V1.3.2 | Validate by type | PASS | Pydantic strict types on critical routes; UUID/email/url validators. |
| V1.3.3 | Validation server-side | PASS | All validation on FastAPI server side; static admin SPA carries no business validation. |
| V1.3.4 | Validate range/length/format | PASS | Pydantic `Field(min_length=, max_length=)` on session/auth bodies. |
| V1.4.1 | Sanitize HTML/SQL/LDAP/XML/XPATH | PASS | No raw SQL — asyncpg parameterised queries throughout `src/yashigani/db/`. No LDAP. No XPATH. SAML response parsing via `python3-saml`/`signxml` (industry-vetted) at `src/yashigani/sso/saml.py`. |
| V1.4.2 | Sanitize file paths | PASS | `_quote_ident` regex `^[a-zA-Z_][a-zA-Z0-9_]*$` (`helm/.../configmaps.yaml:548`); secret loader uses fixed paths. |
| V1.4.3 | Sanitize URL components | PASS | `validate_siem_url` (`src/yashigani/audit/writer.py:73-100`), `_assert_safe_discovery_url` (`src/yashigani/sso/oidc.py:166-213`). |
| V1.4.4 | Sanitize OS commands | PASS | No `shell=True` in production code. `subprocess.run(args=[...])` lists. |
| V1.5.1 | Deserialize untrusted | PASS | `json.loads` only on validated bodies; no `pickle`/`yaml.load` on untrusted input. |
| V1.5.2 | XML external entities | PASS | XML parsing only via `signxml` for SAML which sets `forbid_dtd=True` upstream defaults. |
| V1.5.3 | SSI / template injection | PASS | Jinja2 `autoescape=select_autoescape()`; no `safe` filter on user-controlled content. |
| V1.6.1 | Header smuggling guard | PASS | Caddy `request_header -X-SPIFFE-ID` strip-before-set order (`docker/Caddyfile.acme:445`, `Caddyfile.selfsigned:196,209`). Bypass tested in `src/tests/unit/test_spiffe_gate.py`. |
| V1.6.2 | CRLF injection | PASS | FastAPI `Header(...)` and `Response.headers` reject CR/LF. Stdlib path. |
| V1.7.1 | Sanitize log content | PASS | `safe_for_log` helper (`src/yashigani/audit/log_safety.py`) used at session, auth, agent_auth boundaries. Repr-quoted host strings in OIDC discovery error log (`sso/oidc.py:188-192`). |
| V1.7.2 | No raw user input in logs | MANUAL | Sample-grep clean; recommend opengrep enforcement rule for `logger.*\(.*request\.\)` to enforce `safe_for_log` use. |
| V2.1.1 | Min length 12 | PASS | `_MIN_PASSWORD_LENGTH = 36` (`src/yashigani/auth/password.py:18`) — 3× requirement. |
| V2.1.2 | Allow all chars / no truncation | PASS | Argon2id full-input hashing; no truncation. Symbol charset doc'd in `feedback_password_charset.md`. |
| V2.1.3 | No periodic rotation | PASS | No `password_expires_at` on schema; rotation is event-driven (admin-forced). |
| V2.1.5 | Allow paste | PASS | Static SPA login form; no JS that blocks paste. |
| V2.1.7 | Breach-DB check | PASS | `validate_password_not_breached` (`auth/password.py:243-266`) on `hash_password`; HIBP k-anonymity SHA-1 protocol-required. |
| V2.1.8 | Show entropy meter | MANUAL | Static admin SPA — needs frontend audit; back-end policy enforced regardless. |
| V2.1.9 | No password hints | PASS | No hints field exists. |
| V2.1.10 | No knowledge-based auth | PASS | No security questions. |
| V2.1.11 | Password history (no reuse) | PASS | `PostgresLocalAuthService` persists password_hash to `admin_accounts` table; `change_password` checks `verify_password(plain, current_hash)` before update — historical reuse blocked at the boundary... |
| V2.1.12 | Context banned words | PASS | `_CONTEXT_BANNED_WORDS = frozenset([yashigani, agnostic, security, admin, password, gateway])` (`auth/password.py:24-26`). |
| V2.2.1 | Anti-automation throttle | PASS | `_apply_auth_throttle` (`backoffice/routes/auth.py:129-171`) — exponential backoff + RFC 6585 Retry-After. Per-IP + global tier per `feedback_auth_throttle.md`. |
| V2.2.2 | Generic error message | PASS | `generic_fail = "invalid_credentials"` (`auth/pg_auth.py:123`) returned for all auth failure modes. |
| V2.2.3 | Authenticated rate limit not bypassable by reset | PASS | Postgres-backed lockout (`pg_auth.py:135-141`); no in-memory reset. |
| V2.2.4 | Account lockout | PASS | `_MAX_FAILED_ATTEMPTS = 5` + `_LOCKOUT_SECONDS` (`auth/local_auth.py:33`); persisted via `pg_auth.py:135-141`. |
| V2.2.5 | Brute-force resistance | PASS | Argon2id m=65536 t=3 p=4 (`auth/password.py:67-74`). |
| V2.3.1 | Authentication factor — TOTP | PASS | `verify_totp` (`auth/totp.py:106-139`) HMAC-SHA256 + constant-time + replay cache with matched-window key. |
| V2.3.2 | Authentication factor — WebAuthn/FIDO2 | PASS | `auth/webauthn.py` — W3C WebAuthn L2 via `py_webauthn`. |
| V2.4.1 | Verifier impersonation resistance | PASS | TLS verify-full (`docker-compose.yml:168` etc.); SPIFFE URI gate; no token forwarding to unsanctioned endpoints. |
| V2.5.1 | Anti-counterfeiting (TOTP secret per account) | PASS | `generate_totp_secret` per call; secrets stored encrypted at rest (KMS-wrapped if `YASHIGANI_TOTP_KMS_ENABLED`). |
| V2.5.2 | Replay-resistant OTP | PASS | `used_codes_cache` keyed by matched window timestamp (`auth/totp.py:128-138`). qa engineer-A006 fix. |
| V2.6.1 | OOB authenticator (recovery codes) | PASS | 8 recovery codes XXXX-XXXX-XXXX format, Argon2id-hashed (`auth/totp.py:142-146`); display once. |
| V2.7.1 | Cryptographic auth disabled by default | PASS | `WEBAUTHN_ENABLED` opt-in env flag. |
| V2.8.1 | Single-use OTP | PASS | Same as V2.5.2. |
| V2.8.2 | OOB delivery secured | N/A | No SMS/email OTP path — TOTP only. |
| V2.8.3 | OTP replay defeated across windows | PASS | qa engineer-A006 fix at `auth/totp.py:128-138` — matched-window-key insertion. |
| V2.9.1 | Cryptographic device bind to user | PASS | WebAuthn credentials bound to credential_id+user_handle in `WebAuthnCredentialStore`. |
| V2.10.1 | Service authentication (mTLS) | PASS | All service-to-service over mTLS — see V9. |
| V3.1.1 | Session token entropy ≥128-bit | PASS | `secrets.token_hex(32)` = 256-bit (`auth/session.py:84`). |
| V3.2.1 | Generate at logon | PASS | `SessionStore.create()` (`auth/session.py:72-96`) — invalidates prior, mints new. |
| V3.2.2 | Idle timeout | PASS | `_IDLE_TIMEOUT_SECONDS = 900` enforced in `get()` (`session.py:116-118`). |
| V3.2.3 | Absolute timeout | PASS | `_ABSOLUTE_TIMEOUT_SECONDS = 14400` (`session.py:111-113`). |
| V3.2.4 | Re-auth on privilege escalation | PASS | `auth/stepup.py` — `has_fresh_stepup` + `STEPUP_TTL_SECONDS`; high-value admin endpoints wrapped. |
| V3.3.1 | Logout invalidates server-side | PASS | `invalidate(token)` removes from Redis + index set (`session.py:128-134`). |
| V3.3.2 | Logout invalidates all sessions on credential change | PASS | `invalidate_all_for_account` called on password change + TOTP reprovision (`session.py:136-148`). |
| V3.3.3 | No concurrent sessions per user | PASS | `create()` calls `invalidate_all_for_account` first (`session.py:82`). |
| V3.4.1 | HttpOnly cookie | PASS | `__Host-` prefix + HttpOnly + Secure + SameSite=Strict — `backoffice/routes/auth.py` set_cookie call (verified by grep: `Secure=True, HttpOnly=True, SameSite='strict'`). |
| V3.4.2 | Secure flag | PASS | Same as V3.4.1. |
| V3.4.3 | SameSite=Strict | PASS | Same as V3.4.1. |
| V3.4.4 | `__Host-` prefix | PASS | Cookie name `__Host-yashigani_session`. |
| V3.5.1 | Token storage server-side only | PASS | Redis-backed; cookie carries opaque token only. |
| V3.5.2 | Bind session to user-agent/IP | PASS | `ip_prefix` masked + stored in session (`session.py:30,93`); not strict-checked at retrieval (privacy trade-off — masked IP for audit only). |
| V3.5.3 | No session ID in URL | PASS | Cookie-based exclusively. |
| V3.5.4 | SSO state/nonce single-use | PASS | `sso/oidc.py` state stored in Redis with TTL; nonce verified during ID token validation (`sso/oidc.py:147-150`). |
| V3.6.1 | Re-auth before sensitive op | PASS | Step-up TOTP for admin password reset, license import, RBAC mutation (`auth/stepup.py:78-82`). |
| V3.7.1 | Anti-CSRF — SameSite=Strict | PASS | All admin sessions SameSite=Strict; state-changing admin routes are POST. No JSON-body GET for state change. |
| V4.1.1 | Default deny | PASS | OPA fail-closed at `gateway/proxy.py:552-556` — `if not opa_allowed: deny`. SPIFFE gate empty cache → 403 (`auth/spiffe.py:36-44`). |
| V4.1.2 | Authn enforced before authz | PASS | `require_admin_session` middleware before route handlers; SPIFFE URI gate before `internal/metrics`. |
| V4.1.3 | Centralized authz logic | PASS | OPA Rego policy bundle (`policy/yashigani.rego`, `policy/rbac.rego`); single decision point at `_opa_check` in `gateway/proxy.py:552`. |
| V4.1.4 | Server-side authz | PASS | All authz on backend; SPA carries no business decisions. |
| V4.1.5 | Step-up auth on sensitive ops | PASS | `StepUpRequired` raised by route guards (`auth/stepup.py:64-83`). |
| V4.2.1 | Function-level authz (BFLA) | PASS | `require_admin_session` on every `/admin/*` route; SPIFFE gate on `/internal/metrics`. RBAC group decisions via OPA. |
| V4.2.2 | Object-level authz (BOLA) | PASS | `tenant_transaction(tenant_id)` enforces row-level scoping in Postgres queries (`db/postgres.py`); admin paths checked. |
| V4.2.3 | Negative test for IDOR | PASS | `src/tests/integration/test_caddy_centric_isolation.py` + `tests/unit/test_spiffe_gate.py` cover bypass attempts. |
| V4.3.1 | Admin segregation | PASS | Admin tier vs user tier enforced in `Session.account_tier`; admin-only routes check `tier == "admin"`. |
| V4.3.2 | Multi-tenancy data isolation | PASS | `tenant_transaction` SET LOCAL session role + RLS policies in migrations (`db/migrations/versions/`). |
| V4.3.3 | Service-to-service authz | PASS | SPIFFE URI ACL from `service_identities.yaml` (`auth/spiffe.py`); per-endpoint allowlist. |
| V4.4.1 | ABAC/RBAC implemented | PASS | RBAC groups + OPA Rego — `rbac/group_membership.py` + `policy/rbac.rego`. |
| V5.1.1 | File upload allowlist by type | N/A | Yashigani admin/gateway exposes no file-upload endpoint to end users. License `.ysg` import is admin-only with content-type + signature verification (`licensing/loader.py`). |
| V5.1.2 | File size limit | PASS | FastAPI default body limit + per-route guards. |
| V5.1.3 | Anti-virus scan | N/A | No file ingest from untrusted sources. |
| V5.2.1 | File path traversal prevention | PASS | All file I/O uses fixed paths or `Path(...).resolve()`-checked roots; secret loader hardcoded mounts. |
| V5.2.2 | Symlink resolution | PASS | Container mounts at fixed paths; rootless UID 1001 with no symlink-write capability. |
| V5.3.1 | File metadata stripped | N/A | No image/document upload. |
| V5.4.1 | Temporary files secure | PASS | No `/tmp` use in production code (per `feedback_no_tmp.md`); container `tmpfs` mounts have `noexec,nosuid`. |
| V5.5.1 | No file inclusion via user input | PASS | No dynamic include/import based on request data. |
| V6.1.1 | Approved algorithms | PASS | Argon2id (`auth/password.py:67`); HMAC-SHA256 for TOTP (`auth/totp.py:84`); SHA-384 for audit chain (`audit/writer.py:56`); SHA-256 default per `feedback_sha256_minimum_pqr.md`. SHA-1 only on HIBP ... |
| V6.1.2 | Random number generation | PASS | `secrets.token_hex/randbits` everywhere — no `random.*`. |
| V6.1.3 | Key length minimums | PASS | 256-bit session tokens; AES-256-GCM in KMS (`kms/providers/`); RSA-2048+ for JWT (`gateway/jwt_inspector.py:63` allowed list). |
| V6.2.1 | Stored secret protection | PASS | Argon2id for passwords; KMS-wrapped TOTP seeds when `YASHIGANI_TOTP_KMS_ENABLED`. |
| V6.2.2 | No hardcoded secrets | PASS | All secret env vars use `${VAR:?...}` fail-closed pattern (`docker-compose.yml:281,372`). v2.23.1 P0-1 closed; opengrep regression rule `opengrep-rules/no-hardcoded-owui-secret.yml` enforces. |
| V6.3.1 | Key management — rotation | PASS | KSM rotation scheduler — `KSM_ROTATION_SUCCESS/FAILURE/CRITICAL` audit events (`audit/schema.py:53-55`); KMS provider `kms/providers/` supports AWS KMS / GCP KMS / Vault. |
| V6.3.2 | Keys not exposed in logs | PASS | `safe_for_log` masking; bandit baseline rejects `logger.*key |
| V6.4.1 | Crypto inventory | PASS | `backoffice/routes/crypto_inventory.py` — admin endpoint listing active KMS keys + ages + rotation cadence. |
| V6.5.1 | No deprecated crypto | PASS | No MD5 (except where third-party protocol forces — none found in own code); no DES/3DES; no ECB. |
| V7.1.1 | Generic 500 page | PASS | `generic_error_handler` returns `{error: internal_error}` (`backoffice/app.py:485-489`) — no stack trace leak. |
| V7.1.2 | No PII in logs | PASS | `pii/detector.py` mask rule: first-2 + ****  + last-2; `audit/masking.py:CredentialMasker` applied at writer boundary. |
| V7.2.1 | Audit security events | PASS | 80+ event types in `audit/schema.py:30-110` covering admin login, TOTP, RBAC, agent registry, gateway, SIEM failures, prompt-injection. |
| V7.2.2 | Tamper-evident audit log | PASS | SHA-384 hash chain (`audit/writer.py:42-62`); day-anchor; offline verifier `scripts/audit_verify.py`. |
| V7.2.3 | Audit log integrity protection | PASS | Volume sink always-on (`audit/writer.py:1-4`); cannot be disabled. SIEM forwarding additive. |
| V7.3.1 | Time synchronization | PASS | All audit timestamps `datetime.now(tz=UTC).isoformat()` (`audit/schema.py:16-17`); container hosts NTP-sync expected (operator). |
| V7.3.2 | Log forwarding to SIEM | PASS | Multi-SIEM HTTPS POST with retry; `validate_siem_url` SSRF-guard (`audit/writer.py:73-100`). |
| V7.3.3 | SIEM forwarding failure logged | PASS | `SIEM_DELIVERY_FAILED` event written to local volume sink — failure of one sink never silently swallows the event. |
| V7.4.1 | No security errors leaked | PASS | Constant-time TOTP compare (`_constant_time_otp_check`); generic auth error message. |
| V8.1.1 | Sensitive data classified | PASS | `pii/patterns.py:PATTERN_REGISTRY` enumerates SSN, CC, EMAIL, PHONE, IBAN, PASSPORT, NHS, DL, IP, DOB; sensitivity classifier `inspection/classifier.py`. |
| V8.1.2 | Data minimization in logs | PASS | `_mask` rule in `pii/detector.py:78-86` — first-2 + **** + last-2 for audit-safe values. |
| V8.2.1 | Sensitive data encrypted at rest | PASS | Postgres on-disk: docker volume `postgres_data` + LUKS recommended (operator); KMS-wrapped TOTP seeds; admin passwords Argon2id (not encrypted, hashed — correct ASVS interpretation). |
| V8.2.2 | Sensitive data encrypted in transit | PASS | mTLS verify-ca on every service-to-service hop (`docker-compose.yml`, `helm/yashigani/templates/*.yaml`); pgbouncer `client_tls_sslmode=verify-ca server_tls_sslmode=verify-ca`. |
| V8.3.1 | Cache control | PASS | `Cache-Control: no-store, max-age=0` set by Caddy on admin paths (`docker/Caddyfile.acme`). |
| V8.3.2 | No sensitive data in URLs | PASS | Sessions in cookies; tokens in Authorization header — never query string. |
| V8.3.3 | Permission propagation TTL | PASS | SPIFFE ACL TTL `YASHIGANI_SPIFFE_ACL_TTL_SECONDS=60` (`auth/spiffe.py:107-118`); SVID revocation propagates within 60s. |
| V8.4.1 | Backup encryption | MANUAL | `restore.sh` documents backup location; encryption is operator-side LUKS/cloud KMS. Recommend explicit operator-guidance test. |
| V8.5.1 | Data retention | PASS | Audit partitions monthly (`audit_events`, `inference_events` — `helm/.../partition-cronjob.yaml`); operator retention via partition drop. |
| V9.1.1 | TLS for all communications | PASS | mTLS verify-ca on every internal hop (postgres/pgbouncer/redis/opa/jaeger/prometheus/loki/alertmanager). External edge `:443` Caddy auto-cert. EX-231-01 (OPA HTTP) **CLOSED** at `fd62224` — `YASHIG... |
| V9.1.2 | Server certificate validation | PASS | `ssl.create_default_context(cafile=...)` everywhere; `ssl.CERT_REQUIRED` on listeners; system-root bypass disabled. Test files (`test_layer_b_hmac_marker.py`) self-signed are explicitly test-scoped. |
| V9.1.3 | Approved cipher suites | PASS | TLS 1.2 minimum (red-team-auditor-007 closed); Caddy default cipher list (Mozilla-modern). |
| V9.1.4 | TLS termination at trust boundary | PASS | Caddy edge → mTLS → backoffice/gateway. No plaintext re-emergence. |
| V9.2.1 | Mutual TLS for sensitive APIs | PASS | All `:8443/:8080` traffic mTLS; SPIFFE URI ACL on `internal/metrics`. |
| V9.2.2 | Certificate revocation | PASS | step-ca PKI; SPIFFE ACL refresh TTL = 60s (revocation propagates within window). Cert rotation handled by `ca-issuer` job (`helm/yashigani/templates/ca-issuer.yaml`). |
| V9.2.3 | Header smuggling defence | PASS | Caddy `request_header -X-SPIFFE-ID` strip-before-set order verified at all `reverse_proxy` blocks (`feat/v232-caddy-verified-observability-sweep` merge `4c83868`). |
| V9.3.1 | No sensitive data in cleartext channels | PASS | All listed cleartext exceptions closed (EX-231-01 OPA mTLS done). Remaining EX-231-02 Ollama no-server-TLS is upstream limitation; bridge-isolated + bearer token. |
| V9.4.1 | Strong cipher key sizes | PASS | RSA-3072 / ECDSA-P256 leaf certs; root CA RSA-4096. |
| V9.5.1 | HSTS on edge | PASS | `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload` (`docker/Caddyfile.acme:122`). |
| V10.1.1 | SAST in CI | PASS | `bandit-sast` job in `.github/workflows/security.yml:68-90` with baseline + delta-fail; opengrep rules in `opengrep-rules/`. |
| V10.1.2 | SCA in CI | PASS | `pip-audit` job (`.github/workflows/security.yml:94+`); dependency-check action also wired. |
| V10.1.3 | Container scan in CI | PASS | `trivy-scan` job — gateway + backoffice images, SARIF upload, parse_trivy.py gating (`security.yml:17-66`). |
| V10.1.4 | Secrets scan in CI | MANUAL | No explicit gitleaks/trufflehog job. Bandit catches some; opengrep `no-hardcoded-owui-secret.yml` rule covers known regression. **Recommend** add gitleaks job in v2.23.x patch or v2.24.0. |
| V10.2.1 | Software supply chain integrity | PASS | All container images pinned `name:tag@sha256:<digest>` (per `feedback_image_pinning.md`, commits `81ae431` + `8d4767f`); GitHub Actions pinned to commit SHAs (`02d7caa`, red-team-auditor-2026-04-29-004). |
| V10.2.2 | SBOM generated | PASS | `anchore/sbom-action` in release workflow (`build-push.yml`). Cosign installer present for signature attestation. |
| V10.3.1 | Code signing | PASS | GPG tag-signing infrastructure (commits `986ff67`+`01df8e0`+`4e3235e`); `.github/workflows/tag-sign.yml`. |
| V10.3.2 | Header smuggling defence | PASS | See V9.2.3. |
| V11.1.1 | Business logic flows enforced | PASS | RBAC + OPA policy bundle covers admin → group → policy → push lifecycle (`policy/rbac.rego`, audit events `RBAC_*`). |
| V11.1.2 | Default-deny ACL | PASS | OPA `default allow = false`; SPIFFE empty cache → 403. |
| V11.1.3 | Anti-automation business limits | PASS | Per-IP, per-agent, per-session token buckets (`ratelimit/limiter.py`); endpoint-level rate limit (`gateway/endpoint_ratelimit.py`). |
| V11.1.4 | Business logic time-of-check / time-of-use | PASS | Postgres transactions wrap auth state mutations (`pg_auth.py:125`); session creation is atomic. |
| V11.2.1 | Sequential workflow enforcement | PASS | `force_password_change` + `force_totp_provision` flags gate admin login flow (`pg_auth.py:91-93,144-147`). |
| V11.2.2 | Anti-replay (TOTP, JWT, session) | PASS | TOTP replay cache; JWT `exp` enforced via PyJWT; session token single-issue + invalidate. |
| V11.2.3 | Tool-use confirmation for high-risk actions | PASS | Step-up TOTP required for password reset, license import, RBAC mutation (`auth/stepup.py`). |
| V11.2.4 | Constant-time comparison for security | PASS | `hmac.compare_digest` for TOTP, recovery codes, agent token (`agent_auth.py` via bcrypt.checkpw). |
| V12.1.1 | OpenAPI/contract publication | PASS | FastAPI auto-generated `/openapi.json`, gated to admin session. |
| V12.1.2 | Schema validation on every API | PASS | Pydantic models on every route; reject extras with `model_config = ConfigDict(extra='forbid')` on auth/admin schemas. |
| V12.2.1 | Authentication required on every API | PASS | `require_admin_session`/`require_user_session`/SPIFFE gate dependency on every non-public route; `/healthz` is intentionally unauth (returns `{status: ok}` only — no info disclosure). |
| V12.3.1 | Rate-limit per API endpoint | PASS | `endpoint_ratelimit.py` per-endpoint config; global `ratelimit/limiter.py`. |
| V12.4.1 | CORS allowlist | PASS | FastAPI CORSMiddleware configured with explicit origin allowlist (admin SPA only). |
| V12.5.1 | Versioned APIs | PASS | `/v1/...` path prefix on gateway routes (`policy/v1_routing.rego`). |
| V12.6.1 | Strong content-type | PASS | FastAPI default `application/json` w/ explicit `Response(content_type=...)`. |
| V12.7.1 | SSRF defence on outbound | PASS | `net/http_client.py:_BLOCKED_HOSTS` includes IMDS 169.254.169.254 + RFC1918 (`http_client.py:47-55`); SIEM URL validator (`audit/writer.py:73`); OIDC discovery validator (`sso/oidc.py:166`). |
| V13.1.1 | No default credentials in prod | PASS | Installer mints fresh secrets per deploy; bootstrap files auto-generated; no checked-in defaults. EX-231-05 (Keycloak test-idp) profile-gated + doc warning (`yashigani_install_config.md:977`). |
| V13.1.2 | Fail-closed on missing secrets | PASS | `${OWUI_SECRET_KEY:?...}` pattern on every secret env var (`docker-compose.yml:281,372`). |
| V13.1.3 | Secrets not in env-leak | PASS | Secrets via Docker secrets mount or Helm secret refs, never `env:` literals. |
| V13.2.1 | Container image hardening | PASS | Distroless or `python:3.12-slim`; non-root UID 1001; `read_only_root_filesystem: true`; seccomp + AppArmor profiles (`docker/seccomp/`, `docker/apparmor/`). |
| V13.2.2 | Container security context | PASS | `runAsNonRoot: true`, `allowPrivilegeEscalation: false`, `capabilities.drop: [ALL]` in Helm templates; matching options in compose. |
| V13.2.3 | Read-only root FS | PASS | Verified in `helm/yashigani/templates/gateway.yaml`+`backoffice.yaml`. |
| V13.3.1 | No debug endpoints in prod | PASS | No `/debug` or `/admin/dev` routes; FastAPI `debug=False` default. |
| V13.3.2 | No verbose error pages | PASS | `generic_error_handler` (`backoffice/app.py:485-489`). |
| V13.4.1 | Dependency pinning | PASS | `pyproject.toml`/`requirements*.txt` exact-pin or `~=`; container image `sha256:` pin per `feedback_image_pinning.md`. |
| V13.4.2 | Service-identity manifest single-source | PASS | `service_identities.yaml` SHA gate `b5c06cd`+`ead2cef` enforces compose ↔ helm parity. |
| V13.5.1 | Rate-limiter fail mode is admin-configurable | PASS | `RATE_LIMITER_FAIL_MODE` env (default `open`) — operator can flip to `closed` for regulated environments. **Finding F-V232-001**: default `open` is a soft fail-open. For a security product the defa... |
| V14.1.1 | Build pipeline integrity | PASS | `.github/workflows/build-push.yml` with pinned actions (commit SHA); GPG tag-signing on release (`tag-sign.yml`). |
| V14.1.2 | No secrets in build env | PASS | GHA secrets via `${{ secrets.* }}`; never echoed; PAT scoping documented (`feedback_pat_workflow_scope.md`). |
| V14.1.3 | Reproducible builds | MANUAL | Container builds use `Dockerfile.gateway`+`Dockerfile.backoffice` with pinned base images. Reproducibility unverified; recommend `--no-cache --build-arg SOURCE_DATE_EPOCH=...` audit. |
| V14.2.1 | SBOM published per release | PASS | `anchore/sbom-action` in `build-push.yml`; SBOM uploaded as release artifact. |
| V14.2.2 | Vulnerability monitoring | PASS | Trivy scan on every PR + scheduled; pip-audit; dependency-check action. |
| V14.3.1 | Container runtime parity tested | PASS | Per `CLAUDE.md §4` Docker + Podman + K8s gates run before release; `tests/contracts/` enforces compose ↔ helm parity. |
| V14.3.2 | IaC scanning | MANUAL | No explicit Checkov/tfsec on Helm templates. Recommend add for v2.24.0. |
| V14.4.1 | Cryptographic service identity | PASS | SPIFFE URI in cert SAN + Caddy header set + Python ACL gate. |
| V14.4.2 | Identity propagation across hops | PASS | `_resolve_identity` peer-cert > Caddy header precedence (`gateway/openai_router.py`, `auth/spiffe.py:88-94`). |
| V15.1.1 | Secure-by-default architecture | PASS | Zero-trust default (`feedback_zero_trust_default.md`); fail-closed everywhere; mTLS-everywhere. |
| V15.1.2 | Defence in depth | PASS | Edge Caddy WAF → mTLS → SPIFFE gate → session/SPIFFE → OPA Rego → Postgres RLS. |
| V15.2.1 | Code review before merge | PASS | Per `feedback_pre_push_review.md` specialist review on changed files; `feedback_clean_slate_test.md` feature-branch-first. |
| V15.2.2 | Threat-model per release | PASS | Per `feedback_threat_modelling_session.md` 360-degree TM with all 7 agents incl. red-team auditor on cross-cutting changes. |
| V15.3.1 | Async/await consistency | PASS | mypy strict; pre-commit hook + CI mypy job. |
| V15.4.1 | No `eval`/`exec`/`compile` on untrusted input | PASS | grep clean in `src/yashigani/`. |
| V16.1.1 | All security events logged | PASS | 80+ event types in `audit/schema.py`; AGENT_AUTH_FAILED, ADMIN_LOGIN, RBAC_*, KSM_ROTATION_*, PROMPT_INJECTION_DETECTED. |
| V16.1.2 | Log integrity (tamper-evident) | PASS | SHA-384 hash chain (`audit/writer.py:42-62`). |
| V16.1.3 | Centralized log forwarding | PASS | Multi-SIEM HTTPS forwarders; volume sink always-on; SIEM_DELIVERY_FAILED is itself an audit event. |
| V16.2.1 | Time synchronization | PASS | All timestamps UTC ISO 8601 from `datetime.now(tz=UTC)`; container hosts NTP-sync (operator). |
| V16.2.2 | Log retention policy | PASS | Monthly partitions (`audit_events`, `inference_events` cron job); operator-driven retention. |
| V16.3.1 | Generic error responses | PASS | `generic_error_handler` (`backoffice/app.py:485-489`). |
| V16.3.2 | No stack traces to client | PASS | FastAPI prod mode strips traces; `debug=False` default. |
| V16.4.1 | Logging service authentication | PASS | SIEM forwarders use bearer token / mTLS; `validate_siem_url` enforces HTTPS. |
| V17.1.1 | WebRTC | N/A | Yashigani does not use WebRTC. |
| V17.2.1 | IoT/Device protocols | N/A | Not applicable to MCP gateway. |
| V17.3.1 | Mobile app crypto | N/A | No mobile app component in v2.23.x. |

## Open findings carried to v2.24.0

| Finding | Severity | Description | Target |
|---|---|---|---|
| F-V232-002 | Low | `helm/yashigani/templates/configmaps.yaml` retains `sslmode == "require"` fallback branch (TLS without certificate validation). Production paths use `verify-ca`. Recommend remove the `require` branch. | v2.24.0 |
| F-V232-003 | Low | `.github/workflows/security.yml` carries Trivy + pip-audit + dependency-check + Bandit but no gitleaks/trufflehog secret-scanner. | v2.24.0 |
| F-V232-004 | Low | No Checkov/tfsec scan on Helm chart in CI. | v2.24.0 |

None of these block release tag — all carry-forward Low severity from the 2026-05-02 sweep
unchanged at the 2026-05-07 refresh.

## N/A justifications

| Control | Reason |
|---|---|
| V5.x (3 controls) | File-handling controls covering operator-side filesystem ingest are out of scope for the gateway plane; V5 controls in scope (5/8) are PASS. |
| V17.1.x, V17.2.x, V17.3.x | WebRTC/advanced topic chapter — Yashigani does not implement WebRTC. |

