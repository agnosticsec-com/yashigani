{{/*
  Auto-generated secrets for services that do not specify an existingSecretName.
  In production, set existingSecretName for each service or use external-secrets-operator.
  These generated secrets use random 32-byte alphanumeric strings.

  UPGRADE SAFETY: All secrets use helm `lookup` to preserve existing values on upgrade.
  randAlphaNum is called ONLY on first install (when the secret does not yet exist).
  This prevents password/key rotation on every `helm upgrade`, which would break
  stateful services (postgres, redis) whose data was initialized with the original value.

  Bug history: randAlphaNum without lookup caused postgres_password to rotate on
  revision 4 upgrade (2026-04-30), breaking gateway SSL auth to pgbouncer because
  the DB user password was initialized with the rev-3 value and was not rotated with it.
  Fixed at tip 54e607a closure (2026-04-30). Reference: secrets.yaml upgrade-safety fix.
*/}}

{{- if not .Values.global.useExternalSecrets }}

{{- if not .Values.postgres.existingSecretName }}
{{- $pgSecret := lookup "v1" "Secret" .Release.Namespace "yashigani-postgres-secrets" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: yashigani-postgres-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
type: Opaque
data:
  postgres_password: {{ if $pgSecret }}{{ index $pgSecret.data "postgres_password" | quote }}{{ else }}{{ randAlphaNum 32 | b64enc | quote }}{{ end }}
{{- end }}

{{- if not .Values.redis.existingSecretName }}
{{- $redisSecret := lookup "v1" "Secret" .Release.Namespace "yashigani-redis-secrets" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: yashigani-redis-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
type: Opaque
data:
  redis_password: {{ if $redisSecret }}{{ index $redisSecret.data "redis_password" | quote }}{{ else }}{{ randAlphaNum 32 | b64enc | quote }}{{ end }}
{{- end }}

{{- if not .Values.gateway.existingSecretName }}
{{- $gwSecret := lookup "v1" "Secret" .Release.Namespace "yashigani-gateway-secrets" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: yashigani-gateway-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
type: Opaque
data:
  jwt_secret: {{ if $gwSecret }}{{ index $gwSecret.data "jwt_secret" | quote }}{{ else }}{{ randAlphaNum 64 | b64enc | quote }}{{ end }}
  hmac_secret: {{ if $gwSecret }}{{ index $gwSecret.data "hmac_secret" | quote }}{{ else }}{{ randAlphaNum 64 | b64enc | quote }}{{ end }}
{{- end }}

{{- if not .Values.backoffice.existingSecretName }}
{{- $boSecret := lookup "v1" "Secret" .Release.Namespace "yashigani-backoffice-secrets" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: yashigani-backoffice-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
type: Opaque
data:
  jwt_secret: {{ if $boSecret }}{{ index $boSecret.data "jwt_secret" | quote }}{{ else }}{{ randAlphaNum 64 | b64enc | quote }}{{ end }}
  hmac_secret: {{ if $boSecret }}{{ index $boSecret.data "hmac_secret" | quote }}{{ else }}{{ randAlphaNum 64 | b64enc | quote }}{{ end }}
{{- end }}

{{- if not .Values.grafana.existingSecretName }}
{{- $grafanaSecret := lookup "v1" "Secret" .Release.Namespace "yashigani-grafana-secrets" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: yashigani-grafana-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
type: Opaque
data:
  grafana_admin_password: {{ if $grafanaSecret }}{{ index $grafanaSecret.data "grafana_admin_password" | quote }}{{ else }}{{ randAlphaNum 32 | b64enc | quote }}{{ end }}
{{- end }}

{{- if and .Values.openWebui.enabled (not .Values.openWebui.existingSecretName) }}
{{- $owSecret := lookup "v1" "Secret" .Release.Namespace "yashigani-open-webui-secrets" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: yashigani-open-webui-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "yashigani.labels" . | nindent 4 }}
type: Opaque
data:
  secret_key: {{ if $owSecret }}{{ index $owSecret.data "secret_key" | quote }}{{ else }}{{ randAlphaNum 64 | b64enc | quote }}{{ end }}
{{- end }}

{{- end }}
