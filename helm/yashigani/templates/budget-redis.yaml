{{- if .Values.budgetRedis.enabled }}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: yashigani-budget-redis
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: yashigani-budget-redis
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- include "yashigani.labels" . | nindent 4 }}
spec:
  serviceName: yashigani-budget-redis
  replicas: {{ .Values.budgetRedis.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: yashigani-budget-redis
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: yashigani-budget-redis
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "yashigani.serviceAccountName" . }}
      # /run/secrets mount collision — see redis.yaml note.
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: budget-redis
          image: "{{ .Values.budgetRedis.image.repository }}:{{ .Values.budgetRedis.image.tag }}"
          imagePullPolicy: {{ .Values.budgetRedis.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          {{- if .Values.mtls.enabled }}
          # trust anchor: ca_root.crt — libssl strict-mode (error:0A000086 with intermediate)
          command:
            - "sh"
            - "-c"
            - |
              redis-server \
                --port 0 \
                --tls-port 6380 \
                --tls-cert-file /run/secrets/budget-redis_client.crt \
                --tls-key-file /run/secrets/budget-redis_client.key \
                --tls-ca-cert-file /run/secrets/ca_root.crt \
                --tls-auth-clients yes \
                --tls-protocols "TLSv1.2 TLSv1.3" \
                --maxmemory-policy noeviction \
                --maxmemory 256mb \
                --requirepass "$(REDIS_PASSWORD)" \
                --appendonly yes
          ports:
            - name: redis-tls
              containerPort: 6380
              protocol: TCP
          {{- else }}
          command:
            - "sh"
            - "-c"
            - |
              redis-server \
                --maxmemory-policy noeviction \
                --maxmemory 256mb \
                --requirepass "$(REDIS_PASSWORD)" \
                --appendonly yes
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          {{- end }}
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.budgetRedis.existingSecretName | default "yashigani-redis-secrets" }}
                  key: redis_password
          {{- if .Values.mtls.enabled }}
          readinessProbe:
            exec:
              command:
                - "sh"
                - "-c"
                - |
                  redis-cli --tls --cert /run/secrets/budget-redis_client.crt \
                    --key /run/secrets/budget-redis_client.key \
                    --cacert /run/secrets/ca_root.crt \
                    -p 6380 -a "$REDIS_PASSWORD" ping | grep -q PONG
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 5
          livenessProbe:
            exec:
              command:
                - "sh"
                - "-c"
                - |
                  redis-cli --tls --cert /run/secrets/budget-redis_client.crt \
                    --key /run/secrets/budget-redis_client.key \
                    --cacert /run/secrets/ca_root.crt \
                    -p 6380 -a "$REDIS_PASSWORD" ping | grep -q PONG
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 5
          {{- else }}
          readinessProbe:
            exec:
              command: ["sh", "-c", "redis-cli -a \"$REDIS_PASSWORD\" ping | grep -q PONG"]
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 5
          livenessProbe:
            exec:
              command: ["sh", "-c", "redis-cli -a \"$REDIS_PASSWORD\" ping | grep -q PONG"]
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 5
          {{- end }}
          resources:
            {{- toYaml .Values.budgetRedis.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            {{- if .Values.mtls.enabled }}
            - name: pki-certs
              mountPath: /run/secrets
              readOnly: true
            {{- end }}
      {{- if .Values.mtls.enabled }}
      volumes:
        - name: pki-certs
          secret:
            secretName: {{ .Values.mtls.existingSecretName | default .Values.mtls.secretName }}
            defaultMode: 0440
      {{- end }}
  {{- if .Values.budgetRedis.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.budgetRedis.persistence.storageClass }}
        storageClassName: {{ .Values.budgetRedis.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.budgetRedis.persistence.size }}
  {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: yashigani-budget-redis
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: yashigani-budget-redis
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- include "yashigani.labels" . | nindent 4 }}
spec:
  type: {{ .Values.budgetRedis.service.type }}
  clusterIP: None
  selector:
    app.kubernetes.io/name: yashigani-budget-redis
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    {{- if .Values.mtls.enabled }}
    - name: redis-tls
      port: 6380
      targetPort: 6380
      protocol: TCP
    {{- else }}
    - name: redis
      port: {{ .Values.budgetRedis.service.port }}
      targetPort: 6379
      protocol: TCP
    {{- end }}
{{- end }}
