{{/*
  DEPRECATED (v2.23.2): This standalone Caddy sub-chart and its ConfigMap are
  deprecated. The authoritative Caddy ConfigMap is rendered by the parent
  helm/yashigani chart (helm/yashigani/templates/configmaps.yaml).

  This template is security-incomplete: it lacks mTLS transport,
  inject-caddy-verified on all routes, SPIFFE URI forwarding, TLS 1.3
  enforcement, and security response headers.

  Additionally, both this ConfigMap and the parent chart's ConfigMap use the
  name yashigani-caddy-config — installing both in the same namespace causes a
  Kubernetes conflict. The parent chart's version is authoritative.

  DO NOT enable this sub-chart. It will be removed in v2.24.0.
*/}}
{{- if .Values.enabled }}
{{- fail "helm/charts/caddy is deprecated and must not be enabled. Use the parent helm/yashigani chart. See helm/charts/caddy/Chart.yaml for details." }}
{{- end }}
