replicaCount: 1
image:
  # v2.23.2 Phase 3b: migrated from all-in-one:1.57 to jaeger:2.17.0 (v2).
  # See helm/yashigani/templates/jaeger.yaml for the canonical deployment path.
  repository: jaegertracing/jaeger
  tag: "2.17.0"
  pullPolicy: IfNotPresent
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
persistence:
  size: 5Gi
  storageClass: ""
