# Yashigani Phase 2 dependencies

# Auth
argon2-cffi>=23.1
pyotp>=2.9
qrcode[pil]>=7.4
redis>=5.0

# Inspection pipeline
# LiteLLM for provider-agnostic AI calls (cloud models)
litellm>=1.30
# Ollama Python client (local models)
ollama>=0.2

# Web framework
fastapi>=0.111
uvicorn[standard]>=0.29
httpx>=0.27           # async reverse proxy client

# OPA policy engine client
requests>=2.31        # OPA REST API

# PII detection
presidio-analyzer>=2.2
presidio-anonymizer>=2.2

# APScheduler (also used by KSM rotation)
apscheduler>=3.10
