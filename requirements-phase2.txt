# Yashigani Phase 2 dependencies

# Auth
argon2-cffi>=23.1
pyotp>=2.9
qrcode[pil]>=7.4
redis>=5.0

# Inspection pipeline
# NOTE: litellm, presidio-analyzer, and presidio-anonymizer were removed — zero imports in codebase; not yet used.
# Ollama Python client (local models)
ollama>=0.6.1

# Web framework
fastapi>=0.111
uvicorn[standard]>=0.29
httpx>=0.27           # async reverse proxy client

# OPA policy engine client
requests>=2.31        # OPA REST API

# APScheduler (also used by KSM rotation)
apscheduler>=3.10
